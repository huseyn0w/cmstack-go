# Claude Code personal permissions (do not commit)
.claude/settings.local.json

# Go build artifacts
/cmstack-go
*.exe
*.test
*.out
coverage.out
# Compiled cmd binaries accidentally built at the repo root
/apitoken
/mcp
/server
/worker
/migrate
/seed
/seedcontent

# Local env (the .example templates stay tracked)
.env
.env.*
!.env.example
!.env.prod.example

# Tailwind standalone binary (downloaded, not vendored)
/bin/

# Generated CSS
/web/static/app.css
