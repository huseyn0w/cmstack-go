# CMStack-Go — HANDOFF

Living continuation doc. Refreshed at every milestone. Pairs with `BUILD_PLAN.md` (full plan/stack
mapping/event classification) and the read-only canon `../FEATURE_MATRIX.md` + `../DESIGN_SYSTEM.md`.

**Last updated:** M0–M13 done + **M14 (Email/Notifications) COMPLETE** (pushed). **NEXT = M15** (Analytics & Settings — incl. SEO settings dashboard deferred from M8). Module `github.com/huseyn0w/cmstack-go`. Push with `git push origin HEAD:main` (fast-forward). Latest migration = **00017** (menus); next = 00018 (M12/M13/M14 added no migration).

**M14 (Email/Notifications) shipped:** `internal/platform/mailer` — new `SMTPMailer`+`SMTPConfig` (go-mail v0.8.0), `Noop`, and `Mailer` union interface (4 methods: verify/reset/comment/contact) + `Config` + `New(cfg,log)` factory in `new.go`. Driver select `MAIL_DRIVER` = log(default)|smtp|noop; `smtp` needs SMTP_HOST + a From (MAIL_FROM→AdminEmail fallback), any factory error → logged + falls back to `LogMailer` so both processes still boot. Each method builds multipart/alternative (text+html), all user data `html.EscapeString`-escaped, contact sets Reply-To to submitter (malformed addr logged+skipped, not fatal), send errors wrapped+returned (outbox retries). Injectable `sender func(ctx,*mail.Msg) error` seam swapped via unexported `withSender` in tests → capture wire bytes, no network. Config keys: MAIL_DRIVER, SMTP_HOST, SMTP_PORT(587), SMTP_USERNAME, SMTP_PASSWORD, MAIL_FROM, MAIL_FROM_NAME(CMStack), SMTP_TLS(starttls|tls|none). `buildMailer(cfg,logger)` added to both `cmd/server/main.go` + `cmd/worker/main.go` — one selected instance passed to every listener (assignable to narrower `accounts.Mailer` + web comment/contact adapter ifaces). LogMailer unchanged. build/vet/lint/test green (mailer/accounts/web).

**M13 (Caching) shipped:** (M13-1) `internal/platform/cache` — `Cache` iface (Get/Set(ttl)/Delete/DeleteByPrefix/Clear, []byte values); `Memory` (default, lazy TTL, race-safe), `Redis` (go-redis v9, app key-prefix, SCAN-based prefix-delete — never KEYS/FLUSHDB), `Noop`; `cache.New(ctx, cache.Config)`; config CACHE_DRIVER/CACHE_KEY_PREFIX/REDIS_URL. (M13-2) `internal/web/page_cache.go` PageCache middleware on the public group (AFTER locale+theme so ctx populated): key `page:<locale>:<theme>:<path>`; BYPASS on non-GET / session cookie present (`session.CookieName="cmstack_session"`, now exported) / `HX-Request` / any non-empty query; STORE only 200 + text/html + no Set-Cookie (gob status+CT+body), TTL `CACHE_PAGE_TTL`(60s). Sitemap cached (`sitemap:xml`, 1h); menu `ResolveForLocation` cached (`menu:<loc>:<locale>`, `CACHE_MENU_TTL`10m). Invalidation: `web.CacheInvalidator` registered via `bus.SubscribeSync(posts.EventContentPublished,…)` (runs in-tx in the web process, clears `page:`+`sitemap:`; errors swallowed so publish never rolls back); menu mutations (all 9) clear `menu:`. **Limitation (noted):** unpublish/trash/silent-edit emit no event → bounded by page TTL, not eagerly invalidated. All caches nil-safe (nil = prior behavior).

**M12 (Forms/Contact) shipped:** `internal/contact` — public `/contact` form (GET renders, POST submits) reCAPTCHA v3 + per-IP rate-limit + validation (`mail.ParseAddress`, length caps) → `db.RunInTx` publishes async `contact.submitted` to the OUTBOX (durability; NO submissions table). `NotifyListener` (registered on BOTH server + `cmd/worker` buses via `SubscribeAsyncHandler`) resolves the recipient (settings key `contact_recipient` → `cfg.ContactRecipient`/`CONTACT_RECIPIENT` → `cfg.AdminEmail`; empty → log+no-op) and calls `mailer.SendContactNotification`. `Deps.ContactSvc`; public form uses `@Base`/`SiteConfig.BuildSEO`. Custom form builder deliberately OUT of scope (v1). Reused recaptcha/ratelimit/bus/mailer from the comments module.

**M11 (Menus) shipped:** `internal/content/menus` — migration 00017 (menus w/ partial-unique location, menu_items w/ parent/position/type/ref/url/label, menu_item_translations); service: menu CRUD + location assign, item add/update/delete/`Reorder`, `SaveItemTranslation`, public `ResolveForLocation(location, locale)` (label overlay + base fallback, nested tree by parent_id, internal URLs localized via i18n.LocalizePath, external http URLs passthrough, unassigned location → empty not error). The service does NOT load referenced content — the admin handler resolves slug→url + title→label before AddItem. Admin `/admin/menus` builder (`internal/web/menus_admin.go` + `web/templ/menus_admin.templ`): create menu (name+location), editor with keyboard up/down reorder (MoveItem swaps + Reorder), Alpine type-switched add-item form (post/page/category via narrow lister interfaces reusing postSvc/pageSvc/categorySvc, or custom url), inline en/de/ru label editing. Gated read/create/update/delete:menu. Public header/footer render: `web/templ/menu_view.go` MenuLink source seam (`SetMenuSource`, registered in Router when `Deps.MenuPublicSvc` set) + `menuPublicSource` adapter (reads locale from ctx, maps ResolvedItem→MenuLink); `layout.templ` header emits `@headerMenu(MenuForLocation(ctx,"header"))` + footer `@footerMenu(...,"footer")`. **Drag-sort deferred** (keyboard up/down is the accessible path). Note: header/footer each do a menu DB resolve per public request — a caching candidate for M13.

**M10 (Plugins) shipped:** (M10-1) `internal/plugin` — `Manager` over an immutable registry; `Plugin{Meta, Register(*Hooks)}`; hook types `ActionFunc`/`FilterFunc`/`RegionFunc`; dispatch `DoAction`/`ApplyFilter`/`RenderRegion` iterate ENABLED plugins in registration order, lock-free, per-callback panic recovery. Enable/disable persisted via the M9 settings store through `web.SettingsEnabledStore` (key `plugin:<id>`=1/0; absent → `Meta.DefaultEnabled`). Sample `internal/plugin/samples.ReadingTime` (a `post_content` filter, DefaultEnabled=false). templ: `web/templ/plugin_view.go` source seam (like theme/locale); `layout.templ Base` emits `head` + `body_end` regions via `@templ.Raw` (public only); post detail threads body through the `post_content` filter. `Deps.Plugins *plugin.Manager`. (M10-2) `/admin/plugins` manager (`internal/web/plugins_admin.go` + `web/templ/plugins_admin.templ`, per-row Activate/Deactivate toggle), `read:plugin` GET + `update:plugin` POST, reuses `Deps.Plugins`. Nav "Plugins" (Design group) → `/admin/plugins`. **To add a hook point:** call `mgr.ApplyFilter/DoAction/RenderRegion` at the seam; to add a region, emit `@pluginRegion("<name>")` in templ. **To add a plugin:** implement `plugin.Plugin` + register in `cmd/server` `plugin.NewManager(store, ...)`.

**M9 (Themes) shipped:** (M9-1) FIRST DB-backed settings — `internal/settings` k/v store (migration **00016** `site_settings(key PK,value,updated_at)`, sqlc Get/Upsert/List, cached `Service` clear-on-write; `ActiveTheme`/`SetActiveTheme` helpers) — **M15 extends this store for general/SEO settings**. `internal/theme` registry (default + sepia + noir; `Resolve` = fallback-to-default, `IsValid`, `All`). `.theme-<id>` palette-only token overrides (+ `.dark` variants) in `web/static/tokens.css` → app.css via `make tailwind`. Public `ThemeResolver` middleware on the public route group stores the resolved theme id in context; `layout.templ Base` sets `<html class="h-full theme-<id>">` from a registered `themeSource` (mirrors localeViewSource). Admin never runs the middleware → base palette (isolation). (M9-2) `/admin/appearance` switcher: `internal/web/appearance_admin.go` + `web/templ/appearance_admin.templ` (live per-theme palette preview via a nested `.theme-<id>` box, active badge, activate form), `read:theme` GET + `update:theme` POST, `AppearanceSvc`=settings.Service. Nav "Appearance" (Design group) now points to it.

**M8 (SEO+GEO) shipped, 5 slices:** (1) per-content meta_title/meta_description/canonical_url/noindex on posts/pages/services + per-locale meta_* on the 3 translation tables (migration **00015**, overlay `COALESCE(NULLIF(t.meta_*,''),base)`; canonical/noindex are base-only structural). (2) SEO head: `internal/web/seo.go` `SiteConfig`+`BuildSEO` → `webtempl.SEOView` (nil = legacy minimal head for admin), emits `<title>`/description/canonical/robots/OG/Twitter/hreflang+x-default/verification; env site-identity + GEO/Org fields in config. (3) JSON-LD: one escaper `web/templ/jsonld.go marshalJSONLD`; builders WebSite(+SearchAction)/Organization/BreadcrumbList/ItemList/Service/FAQPage + enriched BlogPosting; emitted via `jsonLDBlocks` (`@templ.Raw`). (4) crawler routes on ROOT router: `/sitemap.xml` (hreflang alts, `kernel.SitemapItem` + `SitemapItems` on posts/pages/services, AllFlat adapters for taxonomy), dynamic `/robots.txt` (AI-crawler toggle via `AllowAICrawlers`, GlobalNoindex staging gate), `/llms.txt`+`/llms-full.txt`. (5) editor SEO panel: shared `seoFields` templ in posts_editor.templ, meta_title/desc on all locale tabs, canonical/noindex gated to default tab via `v.editStructural()`; UpdateInput uses `*string`/`*bool`, CreateInput/TranslationInput plain values. **SEO settings dashboard UI deferred to M15** (fields env-backed now).

**Docker note:** flaky under parallel testcontainers on this machine — run DB tests with `go test -p 2 ./...` (or per-package).

**M7b translation pattern (established, reuse for any future translatable entity):** additive OVERLAY — base row stays default locale (en); `<type>_translations(<id>, locale, <fields>, UNIQUE(<id>,locale))` holds de/ru; reads overlay active-locale with per-field base fallback via `COALESCE(NULLIF(t.field,''), base.field)`; service `SaveTranslation` (rejects default+unsupported locale, permission-gated, sanitize), `GetInLocale`/`PublicBySlugLocale`/`TranslatedLocales`; en path byte-for-byte unchanged; editor per-locale Tab strip via `?language=xx` (structural fields on en tab only; shared web helper `buildLocaleTabs` in `internal/web/categories_admin.go`). Reference impl: `internal/content/posts` (posts_translations) + `pages`/`services`/`categories`/`tags`. Migrations 00011–00014 done: 00011 posts, 00012 pages, 00013 services, 00014 `category_translations`(name,description)+`tag_translations`(name).

**Orchestration note (learned):** big single-agent milestones repeatedly hit the SHARED session token limit mid-run (looks like a "hang" but is throttling). Mitigation: scope each subagent tightly (one bounded slice), keep a compiling committed checkpoint before risky work, and split invasive milestones (e.g. M7b) per content type. Verify every "done" with real `go build/vet/lint/test` before commit; commit+push per milestone.

## Toolchain / how to run
- Go 1.26.4 (`/opt/homebrew/bin/go`). Always `export PATH="$PATH:$(go env GOPATH)/bin"` so templ/sqlc/goose/golangci-lint resolve.
- Tools: templ 0.3.1020, sqlc 1.31.1, goose 3.27.1, golangci-lint 2.12.2, gofumpt 0.10.0, tailwind standalone 4.3.1 (`./bin/tailwindcss`).
- Docker running → testcontainers integration tests run on full `go test ./...` (use `-short` to skip).
- Postgres needs `citext` + `pgcrypto` (migrations enable them).
- Commands: `make generate` (templ+sqlc), `make tailwind`, `make build`, `make test`, `make lint`, `make migrate-up`. Seed: `go run ./cmd/seed`. Server: `go run ./cmd/server`. Worker (outbox relay): `go run ./cmd/worker`.
- Verify gate before any "done": `go build ./... && go vet ./... && golangci-lint run && go test ./...` — all must be green; show output.

## Architecture (hold this line — adversarial review enforces)
`handler (thin: decode→validate→service→render, ZERO logic/data-access) → service (all logic; data only via repo interfaces; side effects only via events) → repository (only sqlc/pgx user) → db`.
Events: `db.RunInTx(ctx,pool,fn)` is the blessed tx seam; `events.Bus.Publish(ctx,tx,evt)`; **sync** listeners run in-tx (atomic, roll back on error), **async** go to the `outbox` table in-tx → `cmd/worker` relay drains post-commit (per-row error isolation + dead-letter after 5 attempts). Explicit constructor DI in `cmd/server/main.go` via `web.Deps`; no globals.
Authz: hand-rolled `accounts.Authorizer.Can(ctx,userID,action,subject)` (Casbin rejected). Permissions `(action,subject)`, `manage`=any action, `all`=any subject; loaded from `role_permissions`, cached with 60s safety TTL.

## DONE
- **M0** foundation: chi router, slog, caarlos0/env config, pgxpool+`RunInTx`, sqlc(pgx/v5)+goose, event bus+transactional outbox+relay, scs sessions, nosurf CSRF, security headers + baseline CSP (prod-gated HSTS), go-playground/validator, templ base `layout.templ` with full DESIGN_SYSTEM tokens (light/dark) + self-hosted woff2 (Newsreader/Inter/Geist Mono), htmx2+alpine3, `/health` + `/health/ready`, Makefile, `.golangci.yml` (0 issues), CI workflow, testcontainers harness.
- **M1** auth (hardened, security-reviewed): users/roles/permissions/role_permissions, email-verification + password-reset tokens (sha256, single-use **atomic consume**, expiring), `password_changed_at` epoch (session invalidation on reset/change), oauth_accounts; idempotent seed (4 roles Administrator/Editor/Author/Member + full permission matrix + default admin from env). argon2id hasher. AuthService: register/login/logout/change-pw/forgot/reset/verify-email with **anti-enumeration** (real dummy-hash + generic errors), signup + email-verify toggles via `SettingsProvider` (config-backed; admin UI=M15). Social login via **goth** (Google+GitHub, enabled only with env keys; `internal/platform/oauth`, routes `/auth/{provider}` + callback). **Admin shell** `web/templ/admin.templ` (AdminBase/sidebar 260px/topbar 56px per DESIGN_SYSTEM §5, permission-gated nav HIDDEN not disabled, dark/light toggle). Profiles + `/account` (name/bio/website/socials + avatar upload + change-password). Public author `/authors/{id}` (no email leak) + ProfilePage/Person JSON-LD (escaped). `internal/platform/storage` (LocalStorage + magic-byte avatar validation, SVG reject, /uploads nosniff+traversal guard). Per-IP rate limiting on auth POSTs. Tests every layer incl testcontainers concurrency.

## Migrations on disk
`00001_init` · `00002_auth` · `00003_auth_hardening` · `00004_oauth` · `00005_posts` · `00006_pages_services` · `00007_taxonomies` · `00008_media` · `00009_comments` · `00010_search` (GENERATED tsvector search_vector + GIN on posts/pages/services). M7b content translation: `00011_post_translations` · `00012_page_translations` · `00013_service_translations` · `00014_taxonomy_translations` (category+tag). M8: `00015_seo_meta` (meta_title/meta_description/canonical_url/noindex on posts/pages/services + meta_* on the 3 translation tables). **Next migration = 00016.**

## DONE since M3 (all pushed to main, tested incl testcontainers)
- **M4 Media**: `internal/platform/storage` (Storage iface, LocalStorage + S3 via aws-sdk-go-v2), `internal/content/media` (upload pipeline, magic-byte validation, SVG/polyglot reject, decompression-bomb guard, thumbnails+dims, per-asset metadata), admin library grid, editor media picker (filled M2a seam), bulk delete. + 3 LOW hardening (read:media in List, files-per-upload cap 8, PDF=attachment).
- **M5 Comments**: `internal/content/comments` (threaded tree, status PENDING/APPROVED/SPAM/TRASH, guest+auth submit, anti-enumeration, sanitize, self-edit window, bulk moderation, public serializer strips email/ip), `internal/platform/recaptcha` (v3 graceful no-op), 8/min rate limit, `comment.created` async → notification email (LogMailer; SMTP=M14), admin `/admin/comments` moderation (status tabs + pending badge), public thread+form (htmx partial on post page).
- **M6 Search**: `internal/content/search` — PG FTS (GENERATED tsvector, GIN) UNION across published posts/pages/services + ts_rank + ts_headline snippet (bluemonday mark-only) + ILIKE fallback; public `/search` results page + header search affordance. Seams: `// TODO(M7 locale)`, `// TODO(M8 noindex)`.
- **M7a i18n foundation** (additive): `internal/platform/i18n` (Locale en/de/ru, embedded catalogs, Translator fallback en→key, {name} interp, as-needed routing helpers, Alternates=hreflang seam); web outer-wrap locale middleware (strips /de,/ru; admin stays en); `<html lang>` + accessible header language switcher preserving page.

## DONE since M1
- **M2** content: kernel (`internal/content/kernel`: status DRAFT/PUBLISHED, slug+dedupe, bluemonday sanitizer pinned, reading-time, generic `revisions` snapshot+restore). **Posts** (`internal/content/posts`): full CRUD, publish-once/preserve, revisions+restore UI, trash/restore/permanent-delete, scheduled publish (worker periodic `PublishDue`), likes, reading time, self-hosted rich-text editor island (Alpine+contenteditable, no Node/CDN) + write-time sanitize, **ownership enforced in service** (Author=own only). **Pages** (hierarchy+cycle-prevention, template selector allow-list, revisions, trash). **Services** (GEO type: summary/price/area_served + ordered `service_faqs`, JSON-LD seam→M8). **Bulk actions** (`internal/web/bulk_admin.go` + `web/templ/bulk_admin.templ`): select-all + bulk trash/restore/publish across posts/pages/services, per-id perm/ownership re-checked. Public: `/blog` index + `/blog/{slug}` article, BlogPosting JSON-LD seam. Adversarial-reviewed (clean; minor fixes applied). Events: `content.revision.created` sync, `content.published` async.
- **M3** taxonomies: **categories** (`internal/content/categories`: self-ref tree, cycle prevention, slug dedupe, BuildTree) + **tags** (flat); `post_categories`/`post_tags` M2M assigned **in the post write tx**; admin tree+parent-picker, editors, bulk delete; public `/categories/{slug}` + `/tags/{slug}` archives, `?category=&?tag=` filters on `/blog`, taxonomy pills on post detail, **related-posts** block. Subjects `category`+`tag` added to authorizer + seed.

## PENDING (ordered — resume here)
- **M7b Content translation** (NEXT — invasive; do per content type, verify each) — move translatable fields (title/body/excerpt/meta) of posts→pages→services→categories/tags into `<type>_translations` (content_id, locale) tables; structural fields (slug/status/author/dates) stay on base row; reads join active-locale translation with **fallback to default locale**; writes per-locale; **dashboard per-locale Tab strip** on content edit forms (DESIGN_SYSTEM Tabs "primary use: per-locale content editing"). Use the M7a `i18n.Locale`/context. Migration 00011+. Keep M2/M3 tests green.
- **M8** SEO+GEO (fill BlogPosting/Service/FAQPage JSON-LD seams; per-content meta/canonical/noindex — translatable, coordinate w/ M7b; sitemap+hreflang using M7a `Alternates`; robots; llms.txt; verification tags; SEO+GEO settings dashboard) · **M9** Themes (runtime-swappable, scoped `.theme-<id>` CSS vars) · **M10** Plugins (hook registry actions/filters/render-regions, discovery+toggle, admin UI, sample reading-time) · **M11** Menus (drag+keyboard builder, per-locale, public render) · **M12** Contact form+email · **M13** Caching (Redis; in-proc ratelimit→Redis; page/fragment cache invalidate on publish) · **M14** Email (SMTP via go-mail; LogMailer now) · **M15** Analytics(GA4/GTM)+Settings UI (SettingsProvider config-backed now) · **M16** RSS/feeds · **M17** REST API + MCP (OAuth 2.1, port ts 48 tools) · **M18** security sweep · **M19** UI/Lighthouse ≥95 · **M20** coverage+E2E+CI. (Full detail in BUILD_PLAN §4.)

## Carry-over notes / seams for later milestones
- `RequirePermission` middleware exists+tested but only `/admin` is gated so far — **M2 must gate every admin content route + action**, and add **ownership scoping** so Author `update:post` = own posts only (seed grant is coarse; enforce in post service).
- Author public page `Posts` is an empty `TODO(M2)` seam — fill with author's published posts in M2.
- Mailer is dev `LogMailer` (logs verify/reset links); real SMTP = M14. SettingsProvider is config-backed; admin Settings UI = M15. Rate limiter is in-proc; Redis option = M13.
- Relay dispatch path is real (worker drains outbox → listeners). Email listener registered.

## Open questions
- None blocking. Stack choices settled (see BUILD_PLAN §2). Proceeding autonomously per user directive; ask only on irreversible/product decisions the canon doesn't answer.

## Continuation prompt (paste into a fresh window)
> Resume building CMStack-Go (`/Users/huseyn0w/Desktop/SWE/cmstack/cmstack-go`). Read `HANDOFF.md` + `BUILD_PLAN.md` + canon `../FEATURE_MATRIX.md` + `../DESIGN_SYSTEM.md`. You are the ORCHESTRATOR: delegate implementation to subagents (TDD, adversarial verification with 2–3 skeptics per module), keep your context lean, protect the strict layering (thin handlers → services → repos; side effects via events/outbox; sync vs async classification). Go at `/opt/homebrew/bin/go`, `export PATH="$PATH:$(go env GOPATH)/bin"`, Docker running. Verify every "done" with real `go build/vet/test` + `golangci-lint` output — no claims without evidence. Commit per milestone (no co-author trailer). **Respond in Russian; code/docs in English.** Resume from the first PENDING milestone (M2 Content core). Work autonomously; only ask on genuinely critical/irreversible decisions.
